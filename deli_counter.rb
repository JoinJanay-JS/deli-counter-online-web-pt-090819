names = ["Ada", "Grace", "Kent", "Matz"]
katz_deli = []
  
def line(array)
  if array.length >= 1
    newArray = []
    names.each do |name|
      newArray.push ("#{counter}. #{name}")
      counter += 1
    end
      puts "Welcome, #{name}/ You are number #{counter} in line."
    end 
  end 
end
